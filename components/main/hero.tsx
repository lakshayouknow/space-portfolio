import { HeroContent } from "@/components/sub/hero-content";

export const Hero = () => {
  return (
    <div className="relative flex flex-col h-full w-full bg-gradient-to-b from-[#0a0e27] via-[#16213e] to-[#030014]">
      <HeroContent />
    </div>
  );
};
